lasercut
========

gripper: löcher zur befestigung falsch
servo:mount zu klein
halter für die battery

pcb
===

mounting holes where the power connector is
connectors for jst wrong (generate new part)
